apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: port-io-postgresql
  labels:
    app.kubernetes.io/name: postgres
spec:
  serviceName: port-io-postgresql
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: port-postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: port-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_DB
          value: port-io
        - name: POSTGRES_USER
          value: port-io
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: port-io-secret
              key: db-password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U port-io
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U port-io
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "256Mi"
            cpu: "150m"
          limits:
            memory: "512Mi"
            cpu: "300m"
      volumes:
      - name: postgres-storage
        emptyDir:
---
apiVersion: v1
kind: Service
metadata:
  name: port-io-postgresql
  labels:
    app.kubernetes.io/name: postgres
spec:
  clusterIP: None
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app.kubernetes.io/name: port-postgres
